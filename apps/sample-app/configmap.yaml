apiVersion: v1
kind: ConfigMap
metadata:
  name: sample-app-html
  namespace: sample-app
data:
  index.html: |
    <!DOCTYPE html>
    <html>
      <body>
        <h1>Hello from gitops-lab</h1>
        <p>This page is served by an nginx Pod deployed with a plain Kubernetes manifest.</p>
      </body>
    </html>
